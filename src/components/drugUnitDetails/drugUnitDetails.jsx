import React, { PropTypes } from 'react';
import { Modal  } from 'react-bootstrap/lib/';



const drugUnitDetails = (drugUnit) => (
    <Modal   >{drugUnit} </Modal>
);

export default drugUnitDetails;